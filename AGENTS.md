# 作業規約

- 最初に `PROJECT_MAP.md` を読み、関連ファイルだけを調査する。
- 実装後は構文・対象テスト・起動・Discord接続を可能な範囲で確認する。
- 一時障害は有限 retry と既存の自動復旧で扱い、秘密情報をコード・Git・一般ログへ出さない。
- 変更記録は実装と検証が完了してから、実績だけをEmbedで送信する。
- 破壊的な操作は対象と参照を確認してから行う。
